FROM ubuntu

RUN apt-get update && \
    apt-get install -y iproute2 net-tools iputils-ping dnsutils && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*

# Keep container running indefinitely
CMD ["sleep", "infinity"]