# This V1 Dockerfile works on Docker Desktop for Windows, but not in ACI (Azure Container Instances)

FROM ubuntu

RUN apt-get update && \
    apt-get install -y iproute2 net-tools iputils-ping dnsutils && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*

CMD ["bash"]